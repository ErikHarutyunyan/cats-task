import React from 'react';
// Import Style
import { MainWrapper } from './Main.styled';

const Main = ({ children }) => {
  return <MainWrapper>{children}</MainWrapper>;
};

export { Main };
